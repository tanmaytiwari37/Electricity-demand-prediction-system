# API
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.0
pydantic-settings>=2.0
httpx>=0.27

# Data / ML - pinned to the versions the committed ml/models/model.joblib was
# trained with, so the pickled model loads identically on a fresh install (Render).
pandas==2.3.3
numpy==2.4.6
scikit-learn==1.9.0
joblib==1.5.3
holidays>=0.40

# Tests
pytest>=8.0
